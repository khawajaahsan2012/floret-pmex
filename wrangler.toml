name = "floret-pmex"
main = "worker.js"
compatibility_date = "2024-11-06"

# Serve the static files (index.html, etc.) from the repo root,
# and let worker.js handle the /api/* routes.
[assets]
directory = "."
binding = "ASSETS"
